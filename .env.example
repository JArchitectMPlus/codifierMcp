# CodifierMcp Configuration
# Copy this file to .env and fill in your values

# ==================================================================
# Data Store Selection
# ==================================================================
# Choose your backend: "supabase" (default) or "confluence"
DATA_STORE=supabase

# ==================================================================
# Supabase Configuration (required when DATA_STORE=supabase)
# ==================================================================
# Your Supabase project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Service role key (found in Project Settings > API)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: specific project UUID. If omitted, auto-creates a "default" project.
# SUPABASE_PROJECT_ID=

# ==================================================================
# Confluence Configuration (required when DATA_STORE=confluence)
# ==================================================================
# Uncomment and fill in if using Confluence as data store
#
# CONFLUENCE_BASE_URL=https://yoursite.atlassian.net
# CONFLUENCE_USERNAME=your-email@example.com
# CONFLUENCE_API_TOKEN=your-api-token-here
# CONFLUENCE_SPACE_KEY=TT

# Page titles (defaults shown — override if needed)
RULES_PAGE_TITLE=Rules
INSIGHTS_PARENT_PAGE_TITLE=Memory Insights

# ==================================================================
# Transport Configuration
# ==================================================================
# Transport mode: stdio (for local/CLI use) or http (for remote server)
TRANSPORT_MODE=stdio

# HTTP server port (only used when TRANSPORT_MODE=http)
HTTP_PORT=3000

# API authentication token (REQUIRED when TRANSPORT_MODE=http)
# Generate a secure random token: openssl rand -base64 32
# API_AUTH_TOKEN=your-secure-token-here

# ==================================================================
# Logging
# ==================================================================
# Logging Level: debug, info, warn, error
LOG_LEVEL=info
